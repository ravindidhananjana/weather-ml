[project]
name = "weather-ml-api"
version = "1.0.0"
requires-python = ">=3.11"

[tool.fastapi]
entrypoint = "main:app"

[tool.setuptools]
py-modules = ["main", "config"]

[tool.setuptools.packages.find]
include = ["db*", "data*"]